export type Problem = {
    id: number;
    course: string;
    group: string;
    code: string;
    name: string;
    score: number;
    difficulty: number;
};

export type Course = {
    id: number;
    course: string;
    name: string;
};
