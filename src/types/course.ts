export type ICourses = {
    id: string;
    courseCode: string;
    name: string;
    color: string;
}[];

export type Course = {
    id: string;
    courseCode: string;
    name: string;
    color: string;
};
